# What a listing of every alias looks like.  The order differs from the
# reference shell, which lists them in the order of its hash table; this
# lists them by name, so that the output of a script does not depend on
# how the names happened to hash.
alias zz=1
alias aa=2
alias mm=3
alias
